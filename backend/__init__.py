"""
Backend package initializer for the local web security scanner.

This file is intentionally minimal but marks `backend` as a Python package so
that modules like `backend.api` and `backend.run_scans` can be imported.
"""


